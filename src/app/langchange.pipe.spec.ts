import { LangchangePipe } from './langchange.pipe';

describe('LangchangePipe', () => {
  it('create an instance', () => {
    const pipe = new LangchangePipe();
    expect(pipe).toBeTruthy();
  });
});
